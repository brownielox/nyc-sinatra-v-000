<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Figures</title>
  </head>
<h1>Create a Figure</h1>

<form action="/figures" method="post">

  <label>Name: <input type="text" name="figure[name]" id="figure_name"></label>
  <% @titles.each do |title| %>
    <label><input type="checkbox" name="figure[title_ids][]" value="<%= title.id %>" id="title_<%= title.id %>"><%= title.name %></label>
  <% end %>
  <label><em>Add a new title: </em><input type="text" name="title[name]" id="new_title"></label>

  <% @landmarks.each do |landmark| %>
  <label><input type="checkbox" name="figure[landmark_ids][]" value="<%= landmark.id %>" id="landmark_<%= landmark.id %>"><%= landmark.name %></label>
  <% end %>
  <label><em> Add a new landmark: </em><input type="text" name="landmark[name]" id="new_landmark"></label>
  <input type="submit" value="Create New Figure">

</form>
</html>
