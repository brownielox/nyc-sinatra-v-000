<h1><%= @figure.name %></h1>

<h2><em><%= @figure.titles.map { |title| title.name }.join(", ") %></em></h2>

<p>Landmarks:</p>
<ul>
  <% @figure.landmarks.each do |landmark| %>
    <li><%= landmark.name %></li>
  <% end %>
</ul>
